syntax = "proto3";

package server;

import "server/segment.proto";

message UpdateEntity {
  uint32 entity_id = 1;
  double x = 2;
  double y = 3;
  double angle = 4;
  repeated SegmentData segments = 5;
}